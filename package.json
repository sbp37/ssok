{
  "name": "ssok",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "ait:dev": "ait dev",
    "ait:build": "ait build",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit",
    "toss:build": "pnpm build && ait build",
    "toss:deploy": "ait deploy"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "3.4.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.4.0",
    "typescript": "^5.8.0",
    "vite": "^6.3.0",
    "@apps-in-toss/devtools": "3.2.0"
  }
}
